RewriteEngine On

# Quitar .html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Redireccionar de .html a limpio
RewriteCond %{THE_REQUEST} \s/+(.+)\.html[\s?]
RewriteRule ^ /%1 [R=301,L]
